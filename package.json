{
    "type": "module",
    "lint-staged": {
        "*.ts": "eslint --cache --fix",
        "*.{ts,css,md}": "prettier --write"
    },
    "scripts": {
        "prepare": "husky install",
        "lint": "eslint \"src/**/*.ts\" --fix",
        "prettier": "prettier --write src",
        "compile": "tsc && npm run copy",
        "clean": "rimraf dist/",
        "copy": "npx copyfiles -u 1 src/.env src/**/*.json dist/",
        "build": "npm run lint && npm run prettier && npm run clean && npm run compile",
        "start": "npx copyfiles -u 1 \"src/**/*.json\" dist/ && npx copyfiles -f config.json dist/ && node dist/src/index.js",
        "dev": "npm run clean && tsc-watch --onSuccess \"npm run start\"",
        "prod": "npm run build && npm run start"
    },
    "dependencies": {
        "@discordjs/rest": "^2.6.0",
        "@prisma/client": "^7.4.1",
        "discord.js": "^14.13.0",
        "dotenv": "^16.6.1"
    },
    "devDependencies": {
        "@tsconfig/node20": "^20.1.1",
        "@typescript-eslint/eslint-plugin": "^8.56.1",
        "@typescript-eslint/parser": "^8.56.1",
        "eslint": "^8.47.0",
        "eslint-config-prettier": "^9.0.0",
        "husky": "^8.0.3",
        "lint-staged": "^14.0.1",
        "prettier": "^3.0.2",
        "prisma": "^7.4.1",
        "rimraf": "^5.0.1",
        "tsc-watch": "^6.0.4",
        "typescript": "^5.1.6"
    }
}
